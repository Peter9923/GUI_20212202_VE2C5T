<Window x:Class="GameMenu.Windows.Scoreboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameMenu.Windows"
        xmlns:VMS="clr-namespace:GameMenu.ViewModels"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Scoreboard" Height="800" Width="1000">
    <Window.Background>
        <ImageBrush Stretch="Fill" ImageSource="/Images/Menu/game_menu.png" />
    </Window.Background>
    <Window.DataContext>
        <VMS:ScoreboardViewModel/>
    </Window.DataContext>

    <Grid Margin="50 30 50 40">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <ListBox FontSize="15" ItemsSource="{Binding Players}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" FontWeight="Bold" FontFamily="Arial">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="2" >
                        <Grid>
                            <Label Content="{Binding Name}" Margin="25"/>
                            <Label Content="{Binding Score}" Margin="25"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>

            <ListBox.Background>
                <ImageBrush Stretch="Fill" ImageSource="/Images/Menu/galleryForTopButtons.png" />
            </ListBox.Background>

        </ListBox>
    </Grid>
</Window>
